# To list schedules

dagster schedule list -w workspace.yaml --location repository.py

# To start a schedule

dagster schedule start -w workspace.yaml --location repository.py every_five_minutes_weather_data_schedule
dagster schedule start -w workspace.yaml --location repository.py every_hour_weather_data_schedule
dagster schedule start -w workspace.yaml --location repository.py every_minute_weather_data_schedule

# To stop a schedule

dagster schedule stop -w workspace.yaml --location repository.py every_five_minutes_weather_data_schedule
dagster schedule stop -w workspace.yaml --location repository.py every_hour_weather_data_schedule
dagster schedule stop -w workspace.yaml --location repository.py every_minute_weather_data_schedule

# To start the Dagster UI and start a schedule

1. Open the UI using this command in a terminal:
dagster dev -w workspace.yaml
2. Then in another terminal, run the following command:
dagster schedule list -w workspace.yaml --location repository.py
3. In the same terminal from (2), copy one of the “start” commands (i.e. the schedule you want to run)
4. Go back to the terminal from (1), back to the UI, and look at job to confirm the schedule is running
5. If you want to stop this schedule, go back to the terminal where you entered the “start” command (2-3),
and then paste the “stop” command for the same schedule, then go back to the UI.
The job should no longer be running.

# Running pytest
export PYTHONPATH=$PYTHONPATH:/Users/mikiokaji/PycharmProjects/pythonProject/weather_data_pipeline
pytest tests/test_weather_pipeline.py
